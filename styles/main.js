import { StyleSheet } from 'react-native';
import { gray } from './colorsScheme';


const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: gray,
    alignItems: 'center',
    justifyContent: 'center',
  },
});

export default styles;

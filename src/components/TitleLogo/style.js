import { StyleSheet } from 'react-native';
// import { colors } from '../../styles';

export default StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'space-between',
    marginRight: 51, // size drawer icon (35) + her marginLeft (16)
  },
  
  logo: {
    resizeMode: 'contain',
    width: 105,


  },
});

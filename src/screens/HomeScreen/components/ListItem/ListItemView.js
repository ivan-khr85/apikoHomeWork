import React from 'react';
import { View, Text } from 'react-native';


const ListItem = ({
  createdAt,
  description,
  tags,
  title,
  id,
}) => {
  return (
    <View>
      <Text>text</Text>
    </View>
);
  }

export default ListItem;
